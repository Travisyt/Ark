2020.2.7
目前需要改动或者增加映射功能，因为原有的映射功能只能固定识别一个整数类型的包含"id"的字段,其余的字段全都当String类型处理，
然而恰如转账途径中的手续费点数等字段都是非字符类型的，当然我完全可以把他们当作字符类型处理。----已解决

2020.2.8
可以通过在内存库中储存和更新上下文单例，来存储一些以用户令牌为标志的用户环境变量，例如分页大小、当前页面等信息，以减少参数传递，简化编程。

2020.2.16
排序和筛选函数已经完成，就差前端显示了，前端显示函数还没有完成，主要功能是将图标显示和隐藏。